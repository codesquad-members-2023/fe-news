import { CATEGORY } from '@store/news/newsType';

export const TEMP_ID = 'realsnoopso';
export const MAX_ITEM_NUM = 24;

export const INTERVAL_TIME = 5000;
export const DELAY_TIME = 1000;
export const TIMER_INTERVAL_TIME = 1000;

export const CATEGORIES: CATEGORY[] = [
  CATEGORY.GENERAL,
  CATEGORY.BRODCAST,
  CATEGORY.IT,
  CATEGORY.ENGLISH,
  CATEGORY.SPORT,
  CATEGORY.MAGAZINE,
  CATEGORY.REGION,
];

export const SILDE_INTERVAL_TIME = 20000;
