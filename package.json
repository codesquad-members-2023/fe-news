{
  "name": "newsstand",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"sass --watch --style expanded src/style/style.scss style.css \" \"json-server -p 3001 --watch db.json\" "
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
